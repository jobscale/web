apiVersion: autoscaling/v2beta1
kind: HorizontalPodAutoscaler
metadata:
  name: django
spec:
  scaleTargetRef:
    kind: Deployment
    name: django
  maxReplicas: 20
  minReplicas: 1
  metrics:
  - type: Resource
    resource:
      name: cpu
      targetAverageUtilization: 70
