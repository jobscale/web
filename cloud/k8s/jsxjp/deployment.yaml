apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  labels:
    app: jsxjp
  name: jsxjp
  namespace: default
spec:
  selector:
    matchLabels:
      app: jsxjp
  template:
    metadata:
      labels:
        app: jsxjp
    spec:
      containers:
      - image: jobscale/jsxjp
        imagePullPolicy: Always
        name: jsxjp
